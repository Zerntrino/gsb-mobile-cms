export interface InstallmentPlan {
  id: number;
  installmentPlan: string;
  name: string;
  isActive: boolean;
  startDate: string;
  endDate: string;
}

// export enum UserPermission {
//   Promotion = 'promotion',
//   Setting = 'setting',
// }
