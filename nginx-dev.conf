server {
    listen       80 default_server;
    server_name  _;

    #access_log  /var/log/nginx/host.access.log  main;

    location /api {
        proxy_pass http://gsb-api.ubakong.com/api;
    }

    location / {
        try_files $uri $uri/ /index.html =404;
        root   /etc/nginx/html;
        index  index.html;
    }
}